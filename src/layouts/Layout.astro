<!doctype html>
<html lang="en">
	<head>
		<link rel="preconnect" href="https://fonts.googleapis.com">
		<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
		<link rel="preload" as="style" href="https://fonts.googleapis.com/css2?family=Inter:wght@400;700" rel="stylesheet">
		<link rel="preload" as="image" href="/Hlogo.svg">   

		<meta charset="UTF-8" />
		<meta name="viewport" content="width=device-width" />
		<link rel="icon" type="image/svg+xml" href="/favicon.svg" />
		<meta name="generator" content={Astro.generator} />
		<meta name="description" content="Handyman Hinchman - Professional handyman services including plumbing, electrical work, furniture assembly, and more." />
		<title>Handyman Hinchman - Professional Handyman Services</title>
	</head>
	<body>
		<header>
			<nav>
				<div class="logo">
					<a href="/">
						<img src="/Hlogo.svg" alt="Handyman Hinchman Logo" class="logo-image" width="50">
						<span class="logo-text">Handyman <span style="color: var(--accent-color);">Hinchman</span></span>
					</a>
				</div>
				<ul>
					<li><a href="/">Home</a></li>
					<li><a href="/services">Services</a></li>
					<li><a href="/pricing">Pricing</a></li>
					<li><a href="/about">About</a></li>
					<li><a href="/contact"><span id="contact-link">Contact</span></a></li>
				</ul>
			</nav>
		</header>
		<main>
			<slot />
		</main>
		<footer>
			<p>&copy; {new Date().getFullYear()} Handyman Hinchman. All rights reserved.</p>
			<p>📞 (123) 456-7890</p>
			<p>✉️ handymanhinchman@gmail.com</p>
		</footer>
	</body>
</html>




<style>
	:root {
		--primary-color: #3b3b3b;
		--secondary-color: #6b6b6b;
		--accent-color: #CA5118;
		--background-color: #dfdfdf;
		--text-color: #242424;
		--text-light: #f8f8f8;
	}
	
	html,
	body {
		margin: 0;
		width: 100%;
		min-height: 100%;
		font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, 'Open Sans', 'Helvetica Neue', sans-serif;
		color: var(--text-color);
		background-color: var(--background-color);
	}
	
	header {
		background-color: var(--primary-color);
		color: white;
		padding: 1rem;
	}
	
	nav {
		display: flex;
		justify-content: space-between;
		align-items: center;
		margin: 0 auto;
	}

	#contact-link {
		color: var(--primary-color);
		background-color: var(--accent-color);
		padding: 0.5rem 1rem;
		border-radius: 0.5rem;
		font-weight: bold;
	}
	
	.logo {
		display: flex;
		align-items: center;
		gap: 0.5rem;
		font-size: 1.5rem;
		font-weight: bold;
		color: var(--accent-color);
	}
	
	.logo-image {
		vertical-align: middle;
	}
	
	.logo-text {
		display: inline-block;
		vertical-align: middle;
		line-height: 1;
		color: var(--text-light);
	}
	
	nav ul {
		display: flex;
		list-style: none;
		gap: 2rem;
		margin: 0;
		padding: 0;
	}
	
	nav a {
		color: var(--text-light);
		text-decoration: none;
		font-weight: 500;
		transition: color 0.3s;
	}
	
	nav a:hover {
		color: var(--accent-color);
	}
	
	main {
		/* max-width: 1200px;
		margin: 0 auto;  */
	}
	
	footer {
		background-color: var(--primary-color);
		color: var(--text-light);
		text-align: center;
		padding: 1rem;
		padding-top: 4rem;
	}

	@media (max-width: 768px) {
		nav {
			flex-direction: column;
			gap: 1rem;
		}

		.logo a{
			margin-bottom: 0.5rem;
			display: flex;
			flex-direction: row;
			align-items: center;
			gap: 0.3rem;
		}

		.logo-text {
			display:flex;
			flex-direction: column;
			align-items: left;
			gap: 0.15rem;
		}

		nav ul {
			flex-wrap: wrap;
			justify-content: center;
		}
	}

	
	@media (max-width: 450px) {
		nav ul li{
			padding: 0 1.2rem;
		}
	}
</style>
